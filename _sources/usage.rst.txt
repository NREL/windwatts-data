Usage
=====

Installation
------------

To start using windwatts, the user can clone this `git repository <https://github.com/NREL/windwatts-data/>`_ and the execute following command inside the wtk_led_client directory.

.. code-block:: console

   (.venv) $ pip install .

Cloud setup process for this package is given in the README file of the same repository.

